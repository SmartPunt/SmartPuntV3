# Cob's Rules rebuilt clean app

This is a fully rebuilt minimal version designed to avoid the server-action type issue.

## Vercel env vars
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY

## Deploy
1. Create a new GitHub repo
2. Upload only these files
3. Import into Vercel
4. Add env vars
5. Deploy
